const express = require('express');
const router = express.Router();

const registerScore = require("../UserControllers/ResultController")

router.post("/registerScore", registerScore)

module.exports= router